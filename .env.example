# Copy this file to .env before first run:
#   cp .env.example .env

# Compose project name (container and volume prefix)
COMPOSE_PROJECT_NAME=course

# -------------------------
# Shared PostgreSQL / pg_duckdb
# -------------------------
POSTGRES_ADMIN_DB=postgres
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PASSWORD=postgres

# -------------------------
# Superset
# -------------------------
SUPERSET_PORT=8088
SUPERSET_SECRET_KEY=__CHANGE_ME__
SUPERSET_ADMIN_PASSWORD=admin
SUPERSET_DB_NAME=superset_meta
SUPERSET_DB_USER=superset
SUPERSET_DB_PASSWORD=superset

# -------------------------
# Airflow
# -------------------------
AIRFLOW_PORT=8080
DBT_DOCS_PORT=8081
AIRFLOW_UID=50000
AIRFLOW_ADMIN_USERNAME=airflow
AIRFLOW_ADMIN_PASSWORD=airflow
AIRFLOW_ADMIN_EMAIL=admin@airflow.local
AIRFLOW_DB_NAME=airflow_meta
AIRFLOW_DB_USER=airflow
AIRFLOW_DB_PASSWORD=airflow

# Optional: set a generated Fernet key (kept empty for teaching simplicity)
AIRFLOW_FERNET_KEY=

# -------------------------
# Warehouse database
# -------------------------
# Used by local ETL commands run from the devcontainer.
WAREHOUSE_DB_HOST=postgres
WAREHOUSE_DB_PORT=5432
WAREHOUSE_DB_NAME=warehouse
WAREHOUSE_DB_USER=warehouse
WAREHOUSE_DB_PASSWORD=warehouse

# -------------------------
# Ohuseire ETL defaults (Lecture 4)
# -------------------------
OHUSEIRE_AIR_STATION_IDS=8
OHUSEIRE_POLLEN_STATION_IDS=25

# Optional advanced ETL and DAG tuning variables are intentionally omitted here.
# The code has safe defaults, and the lecture notes call out the few knobs worth
# changing during class.
